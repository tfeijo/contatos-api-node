generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Contato {
  id    String @id @default(uuid()) @db.Uuid
  nome  String
  email String
  cnpj  String

  @@map("contatos")
}
