generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Contato {
  id    Int    @id @default(autoincrement())
  nome  String
  email String
  cnpj  String

  @@map("contatos")
}
